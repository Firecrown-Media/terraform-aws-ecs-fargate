# ECS task, EC2 instance, and ALB security groups need unrestricted egress for
# Docker image pulls (ECR), AWS API calls, and health check connectivity.
AVD-AWS-0104
AWS-0104

# ALB created with alb_internal=false is intentionally public-facing.
# This module is used for public web applications.
AVD-AWS-0053
AWS-0053

# aws_lb_listener.main uses a dynamic redirect block conditioned on var.certificate_arn != null.
# When a cert is provided, the listener performs HTTP_301 redirect to HTTPS (port 443).
# Trivy's static analysis cannot evaluate this Terraform conditional at scan time.
AVD-AWS-0054
AWS-0054
